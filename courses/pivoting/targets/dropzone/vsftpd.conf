# vsftpd configuration for DROPZONE (locked down)

# No anonymous access
anonymous_enable=NO

# Local users only
local_enable=YES
write_enable=NO

# Other settings
listen=YES
listen_ipv6=NO
seccomp_sandbox=NO

# Banner
ftpd_banner=DROPZONE FTP Server - Authentication Required
